{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the settings window. Separate from the main window's: capabilities are scoped by window label, so the bar's permissions do not reach this one, and this one has no business writing the clipboard or dragging itself.",
  "windows": ["settings"],
  "permissions": ["core:default", "core:window:allow-close"]
}
